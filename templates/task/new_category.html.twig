{% extends 'base.html.twig' %}

{% block body %}

    {{ parent() }}

    <p>dodajemy kategorieee</p>
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}

{% endblock %}

{% block title %}

{% endblock %}

