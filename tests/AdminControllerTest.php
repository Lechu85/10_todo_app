<?php

namespace App\Tests;

use App\Repository\UserRepository;
use Symfony\Bundle\FrameworkBundle\Test\WebTestCase;

class AdminControllerTest //extends WebTestCase
{/*
    public function testSomething(): void
    {

		/*
		 * Dopracowac i uruchomić
		 *
	    $client = static::createClient();
	    $userRepository = static::getContainer()->get(UserRepository::class);

	    // retrieve the test user
	    $testUser = $userRepository->findOneByEmail('leszek@todoapp.pl');

	    // simulate $testUser being logged in
	    $client->loginUser($testUser);

	    #Prostsza metoda, poprostu wchodzimy na admina
	    //$client->request('GET', '/admin');

	    #druga wersja, wchodzimy na głównąi dopiero klimay link admina

	    $crawler = $client->request('GET', '/');

	    $link = $crawler->selectLink('Admin')->link(); //link ma w treści Admin

		// use click() if you want to click the selected link
	    $client->click($link);


	    $this->assertResponseIsSuccessful();
	    $this->assertSelectorTextContains('h1', 'Hello Admin!');

		// sprawdzamy ttuaj historie i ciasteczka
	    $history = $client->getHistory();
	    $cookieJar = $client->getCookieJar();//słoik z ciasteczkami


	    //dump($cookieJar);

    }*/
}
